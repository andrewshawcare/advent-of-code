apiVersion: 1

deleteDatasources:
  - name: Tempo

datasources:
  - name: Tempo
    type: tempo
    access: proxy
    orgId: 1
    url: ${GRAFANA_DATASOURCES_TEMPO_URL}
    basicAuth: false
    isDefault: true
    version: 1
    editable: false
    apiVersion: 1
    uid: tempo